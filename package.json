{
	"name": "@zimbra/x-ui",
	"version": "13.2.0",
	"description": "LESS mixins and CSS variables used by the Zimbra X client and Zimbra X compatible zimlets",
	"main": "index.less",
	"keywords": [
		"zimbra",
		"zm-x-web"
	],
	"scripts": {
		"prepublishOnly": "if [ \"$CI\" != \"true\" ]; then git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags; fi"
	},
	"engines": {
		"node": ">=24"
	},
	"repository": "Zimbra/zm-x-ui",
	"license": "BSD-3-Clause"
}
