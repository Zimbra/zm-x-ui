{
	"name": "@zimbra/x-ui",
	"version": "13.0.0",
	"description": "CSS variables, icons, fonts, and legacy LESS helpers used by the Zimbra X client and compatible Zimlets",
	"main": "index.css",
	"style": "index.css",
	"keywords": [
		"zimbra",
		"zm-x-web"
	],
	"scripts": {
		"generateIconVariablesFile": "node generate-icon-variables.js",
		"build": "npm run generateIconVariablesFile",
		"prepublishOnly": "if [ \"$CI\" != \"true\" ]; then npm run build && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags; fi"
	},
	"engines": {
		"node": ">=18"
	},
	"repository": "Zimbra/zm-x-ui",
	"license": "BSD-3-Clause"
}
